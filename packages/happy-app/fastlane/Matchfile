# Private Git repo storing encrypted certificates and provisioning profiles.
# Create at: https://github.com/new (private repo named ios-certificates)
git_url("git@github.com:easyfan/ios-certificates.git")

app_identifier(["com.easyfan.happy"])
username("zheng.easyfan@gmail.com")

# Use API key for authentication instead of Apple ID password (set in lane)
# readonly(true) in CI; readonly(false) when regenerating

storage_mode("git")
