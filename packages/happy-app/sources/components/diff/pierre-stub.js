// Empty stub for @pierre/diffs — the real library is loaded via CDN at runtime on web.
// This file exists only to satisfy Metro's static module resolution.
// It is never executed at runtime because PierreDiffView.web.tsx lazy-imports via
// dynamic import() which resolves to the CDN bundle, not this stub.
module.exports = {};
